import { TriggeroutDirective } from './triggerout.directive';

describe('TriggeroutDirective', () => {
  it('should create an instance', () => {
    const directive = new TriggeroutDirective();
    expect(directive).toBeTruthy();
  });
});
