import { CarryArrDirective } from './carry-arr.directive';

describe('CarryArrDirective', () => {
  it('should create an instance', () => {
    const directive = new CarryArrDirective();
    expect(directive).toBeTruthy();
  });
});
